// JavaScript to handle dynamic skill ring percentages (if needed in future)
document.querySelectorAll('.ring').forEach(ring => {
    const percent = ring.style.getPropertyValue('--percent');
    console.log(Skill: ${ring.previousElementSibling.textContent} - ${percent});
  });